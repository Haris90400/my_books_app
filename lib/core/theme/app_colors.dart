import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  /// Creates Color instance.
  static const Color primary = Color(0xFF3D5CFF);

  /// Creates Color instance.
  static const Color textPrimary = Color(0xFF1F1F39);

  /// Creates Color instance.
  static const Color textSecondary = Color(0xFF858597);

  /// Creates Color instance.
  static const Color textMuted = Color(0xFFB8B8D2);

  /// Creates Color instance.
  static const Color white = Color(0xFFFFFFFF);

  static const Color error = Color(0xFFEB5757);

  static const Color success = Color(0xFF27AE60);
}
